# NEVER prefix with NEXT_PUBLIC_ -- server only [SF-4]
GITHUB_TOKEN=your-github-token

# --- Admin auth (product error logs) ---
# Set both to enable auth on /api/logs/product
# Default: disabled (open access for local dev)
# ADMIN_AUTH_ENABLED=false
# ADMIN_TOKEN=your-secret-token
